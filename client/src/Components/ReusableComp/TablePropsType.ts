// Define the TableHeadingItem interface and exporting
export interface TableHeadingItem {
  label: string;
}

// Types for table data array of objects
export interface TData {
  [key: string]: string;
}

// commany array of object type

export interface ArrayOfObjectType {
  [key: string]: string;
}

// common api respose key value
export interface CommonApiResponseKeyValueType {
  [key: string]: string;
}

// common api respose key value
export interface CommonKeyValueType {
  [key: string]: string;
}
